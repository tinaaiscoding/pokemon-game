// // this add pokemon will be adding pokemon from the battle-ed Opponent.

// // for now lets just say there is a button after the catch, it creates a prompt saying would you like to store it with a btn.

// function addPokemon() {
//     // here we need to fetch the battled pokemon data
//     fetch(`https://pokeapi.co/api/v2/pokemon/${id}`)
//         .then(response =>response.json())
//         .then(response => {
//             let input = []
//             let pokemon = response

//     // after getting details of specific pokemon and we will need to pull the ID of that battled pokemon previously, then we are going to do a post request to update to our server.
// }